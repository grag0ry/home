# /etc/skel/.bash_profile

if [[ $- != *i* ]] ; then
    # Shell is non-interactive.  Be done now!
    return 0
fi

alias tmux="tmux -2"
if [[ -v NERDFONTS ]]; then
    alias eza="eza --icons=always"
fi
